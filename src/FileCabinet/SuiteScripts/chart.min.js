/*
 * Placeholder only.
 * Replace this file with the real Chart.js browser/minified build named chart.min.js.
 * The dashboard will load without the real file, but the chart will show an error message.
 */
window.Chart = window.Chart || null;
